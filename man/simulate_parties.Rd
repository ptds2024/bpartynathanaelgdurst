% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_parties.R
\name{simulate_parties}
\alias{simulate_parties}
\title{Simulate Ice Cream Parties Based on Weather Data}
\arguments{
\item{location}{A character string specifying the city name for weather data (e.g., "Lausanne").}

\item{api_key}{A character string representing the OpenWeatherMap API key.}

\item{n_parties}{An integer specifying the number of parties to simulate (default: 10000).}

\item{epsilon}{A numeric value for the small interval used in surface area calculations (default: 1e-3).}

\item{lower}{A numeric value specifying the lower bound of integration for cone volume and surface area (default: 0).}

\item{upper}{A numeric value specifying the upper bound of integration for cone volume and surface area (default: 10).}

\item{seed}{An integer seed for random number generation to ensure reproducibility (default: 123).}
}
\value{
A data frame containing statistics (mean and standard deviation) for the total volume
and surface area of cones.
}
\description{
Simulates a specified number of ice cream parties based on weather data, estimating the total volume
and surface area of cones required for each party. Weather data is fetched from the OpenWeatherMap API,
and simulations are run in parallel for efficiency.
}
\examples{
\dontshow{if (Sys.getenv("OWM_API_KEY") != "") (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
# Example usage:
api_key <- Sys.getenv("OWM_API_KEY")

# Simulate 100 parties based on weather data for Lausanne
results <- simulate_parties(
  location = "Lausanne",
  api_key = api_key,
  n_parties = 100,
  epsilon = 1e-3,
  lower = 0,
  upper = 10,
  seed = 42
)
print(results)
\dontshow{\}) # examplesIf}
}
